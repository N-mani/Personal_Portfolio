import * as icon from 'react-icons'
import MdOpenInNew from 'react-icons/md'

const ProjectData = (props) => {
  console.log(props.data)
  const { title, date, description, style1, style2, code, liveDemo } =
    props.project
  return (
   
  )
}

export default ProjectData
